<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title + ' - ' : '' %>FlashURL</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <% if (typeof chartjs !== 'undefined' && chartjs) { %>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <% } %>
</head>
<body>
    <nav class="navbar">
        <div class="nav-container">
            <a href="/" class="nav-brand">⚡ FlashURL</a>
            <div class="nav-links">
                <% if (typeof user !== 'undefined' && user) { %>
                    <a href="/dashboard" class="nav-link">Dashboard</a>
                    <a href="/dashboard/links" class="nav-link">My Links</a>
                    <a href="/dashboard/earnings" class="nav-link">Earnings</a>
                    <div class="nav-dropdown">
                        <button class="nav-link dropdown-toggle">
                            <% if (user.avatar) { %>
                                <img src="<%= user.avatar %>" alt="" class="nav-avatar">
                            <% } else { %>
                                <i class="bi bi-person-circle"></i>
                            <% } %>
                            <%= user.name %>
                        </button>
                        <div class="dropdown-menu">
                            <a href="/dashboard/settings" class="dropdown-item">
                                <i class="bi bi-gear"></i> Settings
                            </a>
                            <a href="/dashboard/referral" class="dropdown-item">
                                <i class="bi bi-people"></i> Referrals
                            </a>
                            <hr>
                            <a href="/auth/logout" class="dropdown-item">
                                <i class="bi bi-box-arrow-right"></i> Logout
                            </a>
                        </div>
                    </div>
                <% } else { %>
                    <a href="/auth/login" class="nav-link">Login</a>
                    <a href="/auth/register" class="btn btn-sm">Sign Up</a>
                <% } %>
            </div>
            <button class="nav-toggle" id="navToggle">
                <i class="bi bi-list"></i>
            </button>
        </div>
    </nav>
